import { Injectable, Inject, Injector } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';


@Injectable()

export class SercoRestService {

  

  //constructor(protected http: HttpClient, private injector: Injector, @Inject('BASE_URL') private baseUrl: string) {
  //
  //  //http.get<IUser[]>(this.apiUrl).subscribe(result => {
  //  //  this.valores = result;
  //  //}, error => console.error(error));
  //
  //}

  //getIdSerco(): Observable<IUser> {
  //
  //  return this.http.get<IUser>(this.apiUrl);
  //
  //}


}

